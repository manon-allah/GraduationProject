import 'package:flutter/material.dart';

class CustomAddPostBody extends StatelessWidget {
  const CustomAddPostBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add Post'),
    );
  }
}
